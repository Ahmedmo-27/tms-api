Hi [Developer Name],

Here's some context on the TMS project before your task.

---

Project Overview
Fitness/wellness studio management platform — members, coaches, classes, PT sessions, attendance, packages, payments.

Stack
- Backend: Node.js / Express.js / TypeScript
- Database: MongoDB (Mongoose ODM)
- Auth: JWT Bearer Token (cookie for web, header for mobile)
- Roles: admin, fd, member, coach

---

Existing Attendance Logic
Two attendance record types stored on the Member document:
1. Class attendance (member.attendance[]) — linked to a ScheduledClass via scid
2. PT attendance (member.ptAttendance[]) — linked to a Package via pkgId

---

New Requirement
Implement: GET /admin/attendance

- Auth: Bearer Token, admin or fd role
- Filters (combinable): startDate, endDate, type (CLASS or PT), memberName (partial, case-insensitive)
- Pagination: page, limit (default 25), ordered by attendanceTime desc

Response:
{
  "data": {
    "total": 142,
    "records": [{
      "memberId": "...",
      "memberName": "Ahmed Mostafa",
      "memberPhone": "01012345678",
      "type": "CLASS",
      "className": "Mat Pilates",
      "coachName": "Sara Ahmed",
      "refId": "...",
      "attendanceTime": "2026-06-18T09:00:00.000Z"
    }]
  }
}

Notes
- memberId is the User _id, not the Member _id
- Use Cairo timezone utilities from src/utils/timezone.ts for date filtering
- Follow existing controller/route patterns in the codebase

Let me know if you need any clarification.
