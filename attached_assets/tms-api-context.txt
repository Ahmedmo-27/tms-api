Hi [Developer Name],

Here's a quick overview of the TMS project before your task.

TMS is a fitness/wellness studio management platform handling members, coaches, classes, PT sessions, attendance, packages, and payments.

Stack: Node.js / Express.js / TypeScript, MongoDB (Mongoose), JWT auth (cookie for web, header for mobile). Roles: admin, fd, member, coach.

The Member document holds two attendance record types:
- Class attendance (member.attendance[]) — linked to a ScheduledClass
- PT attendance (member.ptAttendance[]) — linked to a Package

Note: memberId in any response refers to the User _id, not the Member _id. For date filtering always use Cairo timezone utilities from src/utils/timezone.ts — avoid raw UTC comparisons. Follow existing controller/route patterns throughout.

Please have this completed within a day. Let me know if you need any clarification.
